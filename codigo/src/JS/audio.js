var currentAudio = null;

function stopSound() {
  // if (currentAudio) {
  //   currentAudio.pause();
  //   currentAudio.currentTime = 0;
  //   currentAudio = null;
  // }
}
function whooshMid() {
  // if (currentAudio) {
  //   currentAudio.pause();
  //   currentAudio.currentTime = 0;
  //   currentAudio = null;
  // }
  // var audio = new Audio('../audio/whooshMid.mp3');
  // audio.volume = 0.3;
  // audio.play();
  // currentAudio = audio;
}
function whooshLow(e) {
  // if ($(e).hasClass('active')) return;
  // if (currentAudio) {
  //   currentAudio.pause();
  //   currentAudio.currentTime = 0;
  //   currentAudio = null;
  // }
  // var audio = new Audio('../audio/whooshLow.mp3');
  // audio.volume = 0.5;
  // audio.play();
  // currentAudio = audio;
}